let name = 'prerana';
let elements = ['Fire', 'Air', 'Water'];
let fruits = ['apple', 'kiwi', 'mango', 'banana'];
// console.log(name.indexOf('na')); // gives the index of first element we have parsed
// console.log(name.slice(2, 5)); // gives the string from start to end (end not included)
// console.log(name.split('')); // O/P ['p', 'r', 'e', 'r', 'a', 'n', 'a']
// console.log(name.split('', 3)); // O/P ['p', 'r', 'e']
// var str = "How are you doing today?";
// console.log(str.split(" ", 3)); // o/p ['How', 'are', 'you']
// console.log(elements.join(' '));
// console.log(elements.join(',')); //The join() method creates and returns a new string by concatenating all of the elements in an array (or an array-like object), separated by commas (default) or a specified separator string.
// console.log(name.replace('pre', 12));
// console.log(name.charAt(2)); // returns elemnt at given index (starts from 0) same as name[2]

// elements.push('Food');
// elements.pop('')
// elements.shift(); //Removes the first element from an array
// elements.unshift('Goods'); //Inserts new elements at the start of an array.
// console.log(elements);

// let all = elements.concat(fruits);
// console.log(all.reverse());
// console.log(all.sort());

// console.log(all.slice(2, 4));

// let nums = [3, 4, 5, 6, 33, 2, 1, 23, 3, 4, 45, 5, , 43, ];
// // console.log(nums.sort(function(a, b) {
// //     return a - b;
// // })); // returns in ascending order

// console.log(nums.sort(function(a, b) {
//     return b - a;
// })); // returns in descending order